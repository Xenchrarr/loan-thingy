
export enum StatusCode {
    Ok = 1,
    Warning = 2,
    Error = 3,
    WTF = 4,
}


export enum RequestType {
    save,
    check,
    delete,
    edit,
    add,
    download
}

export enum LoanType {
  house,
  car
}
